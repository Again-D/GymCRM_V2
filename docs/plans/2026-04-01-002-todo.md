# UI Readability Improvements Task List

- [x] Unit 1: HeaderLayout button contrast improvement
  - Goal: Ensure logout and runtime control buttons are visible in dark mode.
  - Action: Update `frontend/src/components/layout/HeaderLayout.tsx` to use `type="primary"` for danger button and improve contrast for others.
  - Files: `frontend/src/components/layout/HeaderLayout.tsx`

- [x] Unit 2: MemberListSection status tag visibility
  - Goal: Status tags ("Active", "Hold", etc.) should have high contrast in dark mode.
  - Action: Update `frontend/src/pages/members/components/MemberListSection.tsx` to use `bordered={false}` with solid colors or high-contrast styles for Tag components.
  - Files: `frontend/src/pages/members/components/MemberListSection.tsx`

- [x] Unit 3: Table row selection highlight improvement
  - Goal: Selected rows in tables should have a distinct visual marker (e.g., blue left border) and accessible background in dark mode.
  - Action: Update `frontend/src/index.css` to add custom styles for `.ant-table-row-selected` in dark mode.
  - Files: `frontend/src/index.css`

- [x] Final Review & Verification
  - Run `npm test` on affected pages (Dashboard/Members) to ensure no regressions.
  - Check formatting.
